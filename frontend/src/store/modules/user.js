const state = () => ({
  language: 'english',
  level: 'intermediate',
  sessionId: null,
})

const getters = {}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
}
